version: '3'
services:
  youtube-comments-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - YOUTUBE_API_KEY=  # Deixe em branco e configure via variável de ambiente real
      - PORT=8080
    restart: unless-stopped
